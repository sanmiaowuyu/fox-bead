# 狐狸爱拼豆·喵绘工坊 — 项目交接文档

> 本文档供接手开发者 / 后续维护者快速掌握项目全貌。所有结论均来自线上运行版本（截至 **v138**），可直接照此接手。

---

## 一、项目是什么

**一句话**：一个零依赖的纯前端网页工具，把任意图片转成「拼豆（Perler Beads / 像素豆）」拼装图纸——输出带色号的网格图、用料清单、可下载的 PNG/SVG 图纸。

**商业背景**（重要，影响需求优先级）：
- 用户做的是拼豆定制生意：客户发来照片/图片，用户据此生产拼豆作品。
- 用户当前实际工作流：**客户图（常模糊/背景乱）→ 用豆包（字节）手动生图（提主体/改姿势/换纯色底/加黑边像素风）→ 把生好的图传到本网站 → 转成拼豆图纸**。
- 用户**完全不懂技术**，只管"好不好用 / 哪里别扭"。所有技术决策、bug 修复、部署都由开发者完成，用户只验收体验。
- 工具的卖点是：把"生好的图"一键转成可生产的拼豆图纸（含精准豆数、色号清单）。

---

## 二、产品能力（当前 v138 已具备）

| 模块 | 能力 |
|------|------|
| **图片输入** | 上传本地图 / 粘贴（Ctrl+V）/ 内置示例图（手捧白猫） |
| **板子尺寸** | 5 档：48 / 52 / 78 / 104 / 130（格） |
| **两种上色模式** | 真实模式（默认，饱和度加权均值）/ 卡通模式（众数，大色块感） |
| **自动配色** | 映射到 Mard 221 色标准色卡；各板各模式有动态颜色上限 |
| **去背景** | 手动开关（默认关）；自动识别纯色背景并清为"无豆子"，精准算主体豆数 |
| **像素描边** | 侧栏开关（默认关），给扁平图加外轮廓 |
| **提亮** | 一档提亮开关 |
| **编辑器** | 单格点选换色 / 色号一键替换整片；双击（PC）/ 轻点（手机）选格 |
| **导出** | PNG / SVG 图纸（白底、标题栏 + 色号卡）+ 分享图（小红书海报样式） |
| **双端适配** | PC 与手机均可用（触摸平移/缩放、按钮可点） |

---

## 三、技术架构（铁律级约束）

### 3.1 单文件交付（v132 起，不可破坏）
- **产物只有 1 个 `index.html`**：CSS（style.v5.css）、JS（app.v5.js）、示例图（sample.jpg）、Logo（logo_circle.jpg）、2 张 favicon **全部内联**进 HTML（CSS/JS 内联、图片转 base64）。
- 原因：早期多文件上传（艾可秀/AxureShow）反复漏传本地文件，导致功能全废。**单文件彻底杜绝漏传**。
- 约束：内联后 HTML 里 `</script` 应只有 3 处且都正常闭合（2 处统计 beacon + 1 处主程序）；主程序段内不得含 `</script`。

### 3.2 零依赖、纯前端
- 无任何第三方库、无构建步骤、无后端、无网络请求（除统计 beacon）。
- 颜色计算全用自研 Oklab 实现（`rgbToOklab` / `oklabDist`）。

### 3.3 移动端兼容硬性约束
- 不用 `?.`、不用 `Object.fromEntries`（旧手机浏览器不支持）。
- 子资源加 `APP_VERSION` 破缓存；`viewport user-scalable=no` 防与画布缩放冲突。

### 3.4 关键文件位置
- 源码目录：`fox_vNNN/`（index.html + style.v5.css + app.v5.js + sample.jpg + logo + favicon）
- 交付目录：`fox_vNNN_upload/`（单文件 index.html）
- 工作区根：`C:\Users\小屁孩儿\WorkBuddy\2026-07-08-17-55-32\`
- 版本号常量：`APP_VERSION`（在 index.html 主程序段顶部）

---

## 四、颜色体系（不可更改颜色值）

- 目标色卡 = **Mard 221 色**，分 **A/B/C/D/E/F/G/H/M 九系**（标准色卡原值）。
- 示例：`B23 = #303A21`（深橄榄绿）。
- **铁律**：改色板只增删色号，**绝不重新采样/生成颜色值**；颜色值固定，算法只做"原图色 → 最近标准色"的映射。

---

## 五、核心算法管线

```
processImage
  → buildGrid（最近邻重采样到 N×N，保留每格原图 RGB）
  → mapToPalette（Oklab 最近邻映射到 221 色）
  → reduceColors（computeMaxRegion 特征保护 + outlineProtected 描边保护池 + forceMerge 降级）
  → cleanupNoise
  → applyBrighten（可选，提亮一档）
  → applyOutline（可选，像素描边）
  → renderPreview
```

### 5.1 两模式配色
| 模式 | 取色 | 动态色数上限（板：色数） |
|------|------|------|
| 卡通 | 众数（最频繁色）+ edgeAware 暗部勾边加权（保黑边） | 48→6 / 52→8 / 78→12 / 104→14 / 130→16 |
| 真实（默认） | 饱和度加权均值（REAL_SAT_ALPHA=15.0，平方非线性加权） | 48→16 / 52→20 / 78→24 / 104→26 / 130→30 |

- 由 `recommendMaxColors(mode, N)` 计算，`applyModePreset` 调用，切板子时重算。

### 5.2 特征保留（v119）
- `computeMaxRegion` 洪水算法算每色最大连通区；`reduceColors` 把 `regionSize≥2` 的颜色预保护，防低频特征（如眼睛/鼻子）被合并吞掉。
- 保护集超限时按「频次×反差」打分只保 top-N，杜绝 forceMerge 丢特征。

### 5.3 去背景机制（⚠️ 当前最易出问题，详见第六节）
- `state.bgMask`：二维布尔数组，`true` = 该格为背景填充（导出不画、不计入用料）。
- `bgMode`：`'white'` / `'black'`，默认白底。
- `removeBackground(grid)` 执行识别，受两个保护闸控制（见下）。

---

## 六、去背景 — 重点坑区（接手必读）

### 6.1 工作原理
1. 取内容区域四条边界的原图像素，算亮度中位数作背景基准 `bgLab`。
2. **主闸 mainGate**：边界整体是「亮底（平均 L>0.86）或暗底（L<0.35）」且均匀（vL<0.12）→ 放行。
3. **角落兜底闸 cornerGate**（v137 加）：主闸没过，但画布四角 ≥3 个是明确纯白/纯黑（cmL>0.92 或 <0.20 且 cvL<0.06）→ 仍放行。
4. 两闸都不过 → **整图保留，去背景等于没开**（用户说的"点与不点没变化"即此）。
5. 放行后 BFS 从画布绝对边缘（行0/N-1、列0/N-1）向内填充背景，遇硬边界墙（EDGE_T=42）、形态学保护罩（DILATE_R=2）、安全闸（填充比例>0.85 不填）即停，防吃主体。

### 6.2 已知失效场景（当前待修）
| 场景 | 现象 | 原因 |
|------|------|------|
| 纯黑背景图但「背景」段停在默认「白底」 | 清不掉 | bgColorId 取白，基准比不上黑底 |
| 主体几乎贴满画布 | 无干净留白 → 两闸都失败 | 边界/四角都是主体色 |
| 背景非纯色（渐变/网格/水印/杂色） | 边界太杂 → vL 过大 | 不满足均匀条件 |
| 浅色主体（白猫脸/胳膊） | 被当背景误删 | 阈值/保护罩需再调 |

### 6.3 用户操作提示（写进使用说明）
- 纯黑底图：去背景前先把「背景」段切到 **黑底**。
- 复杂背景图：当前纯前端方案识别不了，需手动或后续接 AI 分割（见第九节）。

---

## 七、部署与交付铁律（不可破坏）

1. **用户只提需求**，开发者改代码 + 打包 + 部署；用户只验收，从不自己部署。
2. **单文件交付**：只交付 1 个 `index.html`，用户只传这 1 个文件（见 3.1）。
3. **CloudStudio 是唯一对外入口**（艾可秀/AxureShow 的 iframe 沙箱禁文件上传、拦截 data/blob URI，已废）。
   - 每次部署生成新 hash 链接：`https://<hash>.<region>.agentos-app.net`，**旧链接立即失效**。
   - **每次交付必须主动把最新网址发给用户**（用户 2026-08-05 明确要求，绝不让他翻历史消息找）。
4. **交付前校验**（单文件版）：
   - 抽取内联 JS 跑 `node --check` 必须通过；
   - grep 功能不变量（APP_VERSION、关键函数名、字体/背景修复标记等）必须全在；
   - 主程序段不得含 `</script` 提前截断。
5. **本地验收**：`file:///.../fox_vNNN/index.html` 硬刷新（Ctrl+F5）；`localhost:8401` 因沙箱隔离打不开。

---

## 八、用户铁律（协作与体验约束）

- **只做本项目**：本工作区不处理其他任务；疑似串台先确认。
- **技术问题直接修**：用户不懂技术，bug/死区/适配缺陷开发者直接修，不必等确认，用户只验收。
- **双端适配铁律**：每次修一个问题，必须同时核对 PC 端 + 手机端都适配、无新问题。
- **不擅自加代码**：只做用户明确要求，不加防御性代码、多余缓存串、try-catch、console.log。
- **根因优先**：定位根因后简单直接改本质，不靠后处理补丁。
- **UI 颜色铁律**：主题/按钮/选中高亮一律贴合既定 CSS 主题变量（`--accent` 浅紫底 / `--accent-ink` 紫字 #6A4C93 / `--accent-soft` / `--accent-line`），不新发明深紫填充、橙圈等脱主题色。
- **交付偏好**：用户不要判断"传哪些文件/是否传全"，出问题就出新版本，用户按版本号更新即可。

---

## 九、生图功能（用户诉求，尚未实现）

- 用户希望把"豆包生图"那一步也集成进网站（一站式：①AI 生图 ②转拼豆）。
- **现状结论**：
  - 转图（去背景）已纯前端解决（v137），零成本 ✓
  - 生图需接图像生成 API（豆包即梦 / 通义万象等），**必须加后端服务器**（API Key 不能放前端）+ 按量计费 + 会破坏"传一个 html 就能用"的单文件部署。
  - 千问 VL 是"理解图片"非"生成插画"，做不出像素风效果，**不适用生图步骤**。
- **下一步待决策**：是否接受加服务器 + 付费；若接受，作为独立模块规划（需另行设计技术方案与成本估算）。

---

## 十、版本史（v118 起关键节点）

| 版本 | 关键内容 |
|------|---------|
| v118 | 130×130 巨幅板 + 真实 30 色 |
| v119 | 特征保留简化 + computeMaxRegion 保护 |
| v119.1 | 头像/Logo 修复 + selfcheck 必备文件校验 |
| v120 | 编辑器三修（清除还原/框选按钮移位/选中框紫+白圈） |
| v121 | 78 板色偏修正（真实 78→24 色） |
| v122 | 卡通偏色修正（保护集封顶 + 卡通动态色数） |
| v123 | 像素风描边（侧栏开关，默认关） |
| v124 | 修描边"开了等于没开"（阈值/外轮廓）——底层 NaN bug 未现 |
| v125 | 描边真正修复（applyOutline 用 `cur[0]` 数字索引取 {L,a,b} 对象 → NaN → 描边失效；改 oklabDist） |
| v126 | 编辑器「色号一键替换」面板 |
| v127 | 删除「框选模式」（用不到）；保留单格点选 + 一键替换 |
| v128 | 背景透明化 + 板子尺寸展示（导出透明底） |
| v129 | 板子卡片还原 + 导出标题栏定格式 |
| v130 | 卡通黑边修复（色数上限大提 + edgeAware + outlineProtected 池） |
| v131 | 真实 104 板色数上限 24→26 |
| v132 | **单文件交付**（根治漏传） |
| v133 | 修复上传点击无反应（显式 click + 粘贴兜底 + 错误提示） |
| v135 | 真因修复 `sy is not defined`（renderCanvas 块级作用域 bug），playwright 真浏览器验证 |
| v136 | 导出黑框修复（PNG/SVG 恢复白底，手机透明区变黑） |
| **v137** | 豆包白底图去背景失效修复（cornerGate + 画布边缘种子） |
| **v138（当前）** | 导出图字体放大 3 倍（标题栏 + 色号卡，PNG+SVG） |

---

## 十一、根因备忘（遇同类 bug 优先查）

- **黑点**：最近邻单点采样落勾边 → 整格黑；卡通 v76 饱和度加权把深棕勾边 → 黑点（v77 撤加权）。
- **暗棕→墨绿(v88)**：mapToPalette 加暗调色相保护 L<0.35 且 a>0 跳过 a<0。
- **背景色切换无效(v89)**：bgMode 只去背景起作用，6 处底色硬编码 → 改动态选色。
- **浅肤色变灰(v95)**：getVisibleFill 把 L>0.92 替换 #F5F5F5 → 改回 PALETTE_BY_ID[id].hex。
- **编辑面板空白(v106)**：stage overflow:hidden + canvas absolute 不撑开父 → 去 overflow + JS 显式尺寸。
- **触摸缩放哆嗦(v112)**：touchmove 每次 getBoundingClientRect reflow → 围绕视图中心 + 去 rect + rAF 节流。
- **触屏焊死/单格(v113)**：重写单指拖=平移、轻点=选格、双指缩放。
- **滑完跑光(v115/v116)**：clampEditPan 约束 pan；修 clamp 死区区间 [vw-dw,0]。
- **手机按钮点不到(v117)**：viewport touchstart preventDefault 吞按钮 click → touch 系列开头 `if(e.target.closest('button'))return;`。
- **78 板色偏(v121)**：recommendMaxColors 78 只 20 色不够 → 提 24。
- **卡通偏色(v122)**：保护集 15 > 预算 8 触发 forceMerge 丢眼鼻嘴 → 保护集封顶 + 卡通动态色数。
- **头像/Logo 404(v119.1)**：cp 漏 6 二进制图标 → 补回 + selfcheck 加必备文件检查。
- **像素描边"开了没用"(v123→v125)**：根因 applyOutline 用 `cur[0]` 数字索引取 {L,a,b} → NaN → 描边永不触发；改 oklabDist。教训：色差计算必须复用全局 oklabDist。
- **导出黑框(v136)**：v128 透明底在手机查看器显示黑色 → 四周黑框 + 字看不清；恢复白底。教训：导出必须白底保证跨设备兼容。
- **去背景失效(v137)**：主闸要求边界均匀，豆包图主体大/贴边/水印 → vL 偏高 → 主闸跳过；加 cornerGate + 画布边缘种子。

---

## 十二、已知问题 & 待办

**已知问题**
- 去背景对纯黑底（未切黑底段）/ 主体贴满画布 / 非纯色背景 仍可能失效（第六节）。
- 示例图仍为手捧白猫（用户 2026-07-20 定"固定不改"；曾想换但 Hello Kitty 有版权风险未换）。

**待办 / 候选**
- 描边粗细控制、颜色选项扩展、清理强度随板子自动调低。
- 拖拽手绘、智能去背景（接 AI 分割）、移动端深度适配。
- 生图模块（见第九节，需加后端 + 付费，待用户决策）。

---

## 十三、如何接手 / 本地验证

1. **改代码**：编辑 `fox_vNNN_upload/index.html`（单文件，所有逻辑在主程序 `<script>` 段）。
2. **升版本**：改 `APP_VERSION` 常量 + 复制为新 `fox_vNNN+1_upload/`。
3. **校验**：
   - `node --check` 抽取的内联 JS；
   - grep 关键不变量（APP_VERSION、功能标记）；
   - 确认主程序段无 `</script` 提前截断。
4. **本地预览**：`file:///.../fox_vNNN_upload/index.html` 硬刷新。
5. **真机/真浏览器验证**（重要）：可用 playwright+chromium 跑 `pageerror` 监听 + 像素采样，确认示例图渲染、上传、编辑器无异常（v135 起此法定位过 `sy is not defined` 真因）。
6. **部署**：CloudStudio 部署单文件目录，得到新 `agentos-app.net` 链接，**主动发用户**。

---

## 十四、一句话交接总结

> 这是「图片 → 拼豆图纸」的纯前端单文件工具，用户不懂技术只管体验，所有技术活由开发者完成并单文件部署到 CloudStudio（每次发新链接）。当前 v138 已实现双模式上色、去背景、编辑器、导出。**最脆弱的环节是去背景识别（纯黑底/贴满画布/非纯色背景会失效），且生图功能尚未实现（需加后端+付费）**。接手后优先稳住去背景鲁棒性，再议生图模块。
