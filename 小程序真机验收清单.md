# 小程序真机验收清单（v143 发版就绪）

> 用途：供余总在本机「微信开发者工具 / 真机」逐项验收。代码层已就绪（单内核、零 DOM、全冒烟 PASS），
> 真机表现需你用自己设备确认。以下每项打 ✅ 即可发版。

## 0. 发版前置（必做）
- [ ] 微信开发者工具导入 `fox-bead/miniapp/` 目录
- [ ] `project.config.json` 的 `appid` 填你自己的小程序 AppID（当前为空，需替换）
- [ ] 工具右上「详情 → 本地设置」勾选「不校验合法域名」（开发期）
- [ ] 重新 `node build.js` 后，`miniapp/utils/core.js` 头部版本应为 `版本: 143`

## 1. 基础链路（P0）
- [ ] 首页能加载，导航栏标题「狐狸爱拼豆」
- [ ] 选图 / 拍照后进入处理页，能出预览图纸
- [ ] 预览网格与实际图案一致（无错位、无空白块）
- [ ] 「保存图片」能导出到相册

## 2. 管线开关（P1，与网页同算法）
- [ ] 模式切换（平均 / 居中 / 边缘）生效
- [ ] 去背景开关：开 → 背景变透明；关 → 保留背景
- [ ] 抖动（Floyd-Steinberg）开/关对比明显
- [ ] 减色上限 slider（4–64）改变后色数随之变化
- [ ] 提亮开关生效
- [ ] 描边开关 + 强度 slider 生效

## 3. 性能与兼容（真机重点）
- [ ] iPhone / 安卓中端机：选 104 板大图，重处理 < 2s 不卡死
- [ ] 低端机（RAM 4G 以下）选图后不闪退
- [ ] 横竖屏切换预览不畸形
- [ ] 微信基础库 3.x 全 PASS（无 `?.` 兼容报错，见铁律 C1）

## 4. 已知非阻塞项（验收时知悉即可）
- 去背景在 `N<=52` 按设计跳过（保留背景），默认 104 不受影响
- 小程序暂未暴露网页端 v143 新增的「保护笔刷 / 补货清单 / 分块PDF」（仅核心算法共用，UI 开关为网页专属，不影响发版）
- 小板想去背景需改 `pipeline-core.js` L458 阈值，按需另排期

## 5. 验收结论
- 真机验收人：__________
- 验收日期：__________
- 结论：☐ 通过发版  ☐ 需修复（列：__________）

---
构建命令（改算法后必跑）：
```
node build.js     # 重新生成 docs/index.html + miniapp/utils/core.js
node tools/smoke-mini.js   # 小程序内核冒烟，须全 PASS
```
